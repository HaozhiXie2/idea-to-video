# 验证记录

2026-09-24，本地离线检查：95 项 Python 测试和 9 组前端检查全部通过。真实 FFmpeg 参与测试，但没有调用收费云服务。

- 三个 5 秒镜头合成为 15 秒、360 帧的无声视频，防止逐镜转码累计丢帧。
- 独立测试环境走通方案、形象、分镜、静态草稿、单镜试拍、剩余批次和最终导出。测试素材不是 AI 生成质量样片。
- 覆盖重复提交、结果不明时停止、任务恢复、版本恢复和局部修改的离线验证。
- 公开代码检查排除运行环境、配置、密钥和生成作品。

尚未完成的验收：本项目自己的文字服务配置、即梦授权及明确授权的小批量真实生成；两个完整工作台同时运行的验证；三名无 AI 视频经验用户的试用。离线通过不代表上述验收已完成，也不保证云端速度与生成质量。
